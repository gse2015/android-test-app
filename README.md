# TestApp

A minimal Android app built with GitHub Actions.

## Build

Push a tag to trigger an automated build:

```bash
git tag v1.0.0
git push origin v1.0.0
```

Or manually trigger the workflow from the **Actions** tab.

## Download

APK artifacts are available in the GitHub Release page after each tagged build.
